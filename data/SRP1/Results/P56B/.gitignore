# Per-evaluation IPOPT solver logs.  Each evaluation gets its own directory so
# that IPOPT's file_append='yes' cannot interleave runs (P5.6-A3 purity); the
# logs themselves are large and reproducible, so only the JSON evidence is kept.
evals/
